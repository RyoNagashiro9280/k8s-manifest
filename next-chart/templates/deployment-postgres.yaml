{{- if .Values.postgresql.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-postgres
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgres
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      # ★★★ ここからが修正部分！ ★★★
      # このPodと、Podがマウントするボリュームのセキュリティ設定
      {{- if .Values.postgresql.podSecurityContext.enabled }}
      securityContext:
        runAsUser: {{ .Values.postgresql.podSecurityContext.runAsUser }}
        runAsGroup: {{ .Values.postgresql.podSecurityContext.runAsGroup }}
        fsGroup: {{ .Values.postgresql.podSecurityContext.fsGroup }}
      {{- end }}
      # ★★★ ここまでが修正部分 ★★★
      containers:
        - name: postgres
          image: "postgres:16"
          imagePullPolicy: "IfNotPresent"
          ports:
            - name: postgres
              containerPort: 5432
              protocol: TCP
          envFrom:
            - secretRef:
                name: {{ .Release.Name }}-postgres-secret
          {{- if .Values.postgresql.persistence.enabled }}
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          {{- end }}
      {{- if .Values.postgresql.persistence.enabled }}
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}-postgres-pvc
      {{- end }}
{{- end }}
